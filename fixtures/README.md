Place your SRX PDF fixtures in this directory when running tests locally.
Optional expected values can be provided in a JSON file with the same basename as the PDF.
These files are ignored by Git and tests will be skipped automatically if no PDFs are present.
